---
# Inventaire Ansible — IBM Kubernetes Service (IKS)
all:
  children:
    iks:
      hosts:
        iks_cluster:
          ansible_connection: local
          ansible_python_interpreter: "{{ ansible_playbook_python }}"
