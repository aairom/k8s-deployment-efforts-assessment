---
# Inventaire Ansible — Red Hat OpenShift on IBM Cloud (ROKS)
all:
  children:
    roks:
      hosts:
        roks_cluster:
          ansible_connection: local
          ansible_python_interpreter: "{{ ansible_playbook_python }}"
