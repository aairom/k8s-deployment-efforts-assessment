# =============================================================================
# Secret — myapp-secret  (SHELL / EMPTY SENTINEL)
# =============================================================================
# ⛔ THIS FILE INTENTIONALLY CONTAINS NO SECRET DATA.
#
# All credentials for MyApp on EKS are managed externally via:
#   AWS Secrets Manager  +  External Secrets Operator (ESO)
#
# The real Kubernetes Secret object is generated at deploy-time by ESO
# from the ExternalSecret defined in:
#   myapp-externalsecret.yaml
#
# DO NOT add base64 values to this file.
# If the Vault Radar / secret scanner flags this file, the scan is passing
# correctly — there is nothing to find here.
#
# See README-secrets.md for full operator setup instructions.
# =============================================================================
#
# This file is intentionally left without a data: block.
# It is kept in the repository only so that the kustomization.yaml
# reference list remains self-documenting.  ESO creates the live Secret
# object automatically once the ExternalSecret and SecretStore are applied.
